name: Build
on:
  push:
    branches:
      #TODO: remove this
      - add-automation
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: '0'
    - uses: xu-cheng/latex-action@v3
      with:
        root_file: |
          resume.tex
